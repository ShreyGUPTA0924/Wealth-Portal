// Request augmentation lives in src/middleware/auth.middleware.ts
// (inline declare module) so it is always loaded by ts-node via the
// import chain: index.ts → auth.routes.ts → auth.middleware.ts
// This file is intentionally left empty.
export {};
